<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect everything to /public except requests to /public/storage
    RewriteCond %{REQUEST_URI} !^/public/storage
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
