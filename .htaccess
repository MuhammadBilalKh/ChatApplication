<IfModule mod_rewrite.c>
    RewriteEngine On

    # If the request is not already pointing to the public directory
    RewriteCond %{REQUEST_URI} !^/public/

    # Redirect everything to public/
    RewriteRule ^(.*)$ public/$1 [L]

</IfModule>
